# rate_limit_invalid — global rate_limit = "abc" (invalid, no unit suffix)

rate_limit = "abc"

[[vm]]
name = "testvm"
base_image = "/var/lib/libvirt/images/testvm.qcow2"
snapshot_dir = "/var/lib/libvirt/snapshots/testvm"

  [[vm.target]]
  path = "/mnt/backup/testvm"
  incremental = true
