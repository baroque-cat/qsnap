# Invalid TOML — malformed syntax (unclosed string)
# This file is intentionally invalid for testing error handling.

name = "testvm
snapshot_dir = "/var/lib/libvirt/snapshots/testvm"

  [[vm.disk]]
  target = "vda"
  base_image = "/var/lib/libvirt/images/testvm.qcow2"
