[Unit]
Description=qsnap — deep verification (qemu-img check on all images)
After=network.target libvirtd.service
Requires=libvirtd.service

[Service]
Type=oneshot
ExecStart=/usr/bin/qsnap -c /etc/qsnap/qsnap.toml check --deep
